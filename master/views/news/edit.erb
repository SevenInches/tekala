<div class="container-fluid">
  <div class="row">
    <div class="col-lg-12">
      <% form_for :news, url(:news, :update,:id => @news.id), :class => 'form-horizontal panel', :style => 'padding:20px 0;' do |f| %>
          <%= partial 'news/form', :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>